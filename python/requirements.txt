# OCI SDK and core dependencies
oci>=2.135.1
requests>=2.32.3
python-dotenv>=1.0.1

# Async HTTP client
httpx>=0.27.0

# Input validation (Pydantic v2)
pydantic>=2.0.0

# FastMCP - Python MCP server framework
# Includes all transport support (stdio, SSE, HTTP) and OAuth authentication
fastmcp>=2.0.0

# HTTP transport dependencies (used by FastMCP for SSE and HTTP modes)
uvicorn>=0.30.0
starlette>=0.38.0

# JWT/OAuth dependencies for OCI IAM authentication
PyJWT>=2.8.0
cryptography>=42.0.0

# Encrypted token storage (for OAuth session persistence)
py-key-value-aio>=0.3.0

# OpenTelemetry (optional, but recommended for OCI APM)
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-instrumentation-requests>=0.50b0
opentelemetry-instrumentation-starlette>=0.50b0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
